import { Box, Typography } from "@mui/material";

const Article = () => {
  return (
    <Box sx={{width: "50%", margin: "30px auto", }}>
      <Typography variant="h6">data</Typography> {/* Display the title */}
    </Box>
  );
};

export default Article;